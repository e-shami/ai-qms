# Memory / Progress Tracker — AI-QMS

> Update this file at the end of every work session. Any AI assistant (or you, months later) should be able to read this file plus `phases.md` and know exactly where things stand — no re-explaining the project from scratch.

## Current Status
- **Stage:** Committee intake/priority integration implemented and tested; Render/Vercel configuration prepared, not deployed. CV is future work and opt-in only. Live ML remains empirical/heuristic with guarded shadow evaluation.
- **Last updated:** 2026-09-25

## Resume Checkpoint 2026-09-25

This section supersedes older completion claims below. Read it before making changes.

### Confirmed Scope And Completed Work
- Hospital-only accessibility requests (elderly/disability) stay normal until an admin or active linked staff member verifies them. One queue: called/in-service first, then approved accessibility, then normal; FIFO within bands. No medical emergency triage was added. Dispatch enforces queue order and one active called/in-service token per counter.
- Public web and WhatsApp intake require a 13-ASCII-digit CNIC string and referral source (website/institution/other organization). Admin intake is optional. Leading zeros preserved. Demo dummy identifiers only; plaintext demo storage is not a production identity system. CNIC/referral excluded from ticket/snapshot outputs and validation error inputs.
- Staff/admin priority badges and review controls, customer issued/live priority status, and WhatsApp status output implemented. UI refresh/session races addressed.
- Returning WhatsApp intake asks explicit same-person confirmation. Backend returns profile_ref and bot now stores/sends that exact reference scoped to institution. New/change-person and global navigation clear it. No automatic identity verification or disclosure of stored CNIC.
- Shared backend/bot JSON fixture tests the real profile contract. Mandatory public intake and bot deployment must roll out together.
- Migration e91a42b7c603 adds nullable intake, normal defaults, review audit fields and check constraints. Isolated PostgreSQL backfill/round-trip/constraints/concurrent issuance/dispatch were verified. No application database migration was performed. If an earlier e91 is already applied, inspect schema and add a forward migration rather than destructive downgrade/reapply.
- Race fix: a token removed from the active queue by a concurrent terminal transition returns null position after refresh instead of an exception after issuance. Count-based numbering replaced with locked maximum suffix allocation; lost-response duplicate issuance remains possible (no idempotency keys).
- Prior uncommitted landing WhatsApp entry/QR, shared select-label fix, consent-based one-off copies and guarded model work retained. User explicitly dropped Phase D lifecycle notifications: no called/served outbox or automatic lifecycle delivery was added. Existing one-off copy feature remains; paid template sending is optional and disabled without approved configuration.

### Demo Deployment Preparation
- Default Compose build/start excludes cv-service via profile `cv`; code retained. Existing CV containers require explicit stop. CI builds backend/frontend/bot only; CV workflow is manual. Oracle automatic CD retired to a manual notice. No remote deployment or push performed.
- render.yaml prepares two free Docker web services with dashboard secrets and CI-gated auto-deploy. Backend respects PORT and migrates before Uvicorn. Vercel uses native Next.js root `frontend`; public URLs/WhatsApp number are build-time configuration. See deployment/README.md.
- .github/workflows, memory.md and phases.md now eligible for tracking so checkpoints and CI survive a pull. Legacy docker-compose.prod.yml remains local/ignored and is not used by Render.
- Tracked .env.example sanitized; actual .env unchanged. Earlier tracked history contained credential-looking values: operator MUST rotate Meta/Upstash credentials before external deployment. Sanitizing the new snapshot does not remove history. No history rewrite authorized/performed.
- Existing QR is intentionally the user-provided demo contact asset; verify its destination before public deployment. Do not claim per-institution WhatsApp sender routing.

### Verification And Limits
- Final CI-command verification: backend compile and 58 unit tests passed; bot TypeScript/build and 100 tests passed; frontend 11 regression tests and lint passed (one existing React Hook Form warning). actionlint passed all workflows. No backend Ruff/mypy or bot ESLint gate is claimed: these are not configured. Fresh installs report dependency advisories, not remediated in this scope.
- Earlier synchronized integration verification: frontend production build passed, 9 isolated Edge intake/public-status browser scenarios passed, disposable PostgreSQL migration/constraints/concurrency passed. HTTP/socket/Meta traffic was mocked except disposable DB tests; no real messages or production writes.
- Latest containerized frontend build retry was blocked when Docker Desktop/WSL failed. Full final Docker image build and GitHub-hosted CI still require verification after recovery/push. Do not label the blocked rerun successful. No Docker/WSL restart forced. A disposable `aiqms-ci-verify` network may remain; remove only that test network if unused.
- Live Render/Vercel setup awaits user accounts, database/Redis configuration, rotated secrets and provider URLs. Render free hours are shared (two continuously running services exceed the allowance), cold starts can delay bot replies, and Vercel Hobby eligibility must be checked. No zero-cost or availability guarantee.

### Next Steps In Order
1. Review/commit the prepared snapshot, then user pushes when ready; do not claim a push or cloud deployment occurred.
2. Recover Docker Desktop, back up application DB, check `alembic current` versus migration history, then coordinated backend/frontend/bot rebuild and demo smoke. Keep CV off. Real Meta end-to-end and approved template delivery remain operator tests.
3. Set up Neon/Upstash, Render backend+bot and Vercel per deployment/README.md; configure exact CORS/webhook/WS URLs, then verify cold starts and service limits. Prefer local Docker for the scheduled demo until hosted flow is verified.
4. Separate future model experiment: user proposed synthetic hospital/bank data. Label all generated data and metrics synthetic, use seeded workload simulation (not independent random targets), hold out scenarios, compare against existing baseline. Do not present synthetic evaluation as real accuracy, overwrite real datasets, or silently promote a model. This experiment has NOT been implemented/retrained in this checkpoint.


## Session 2026-09-17: Consent-Based WhatsApp Token Copies

- Inspected initial dirty worktree, backend/public/admin schemas and issuance, bot signature/status/notification helpers, frontend forms/hooks/types, and the other agent's shared-select label mapping. Preserved pre-existing ML, landing, select, frontend README/types/tests edits. Frontend changes are confined to public join page and admin issue-token form; no staging or commits.
- Added strict `whatsapp_copy` opt-in, default false. Consent requires a normalized full international `+` phone (no country guessing). Public creation keeps its existing rate limiter and never sends messages: requested copies return a server-configured `wa.me` handoff with `status`. Customer must send from their phone; existing signed bot ingress and authenticated phone-scoped active-ticket lookup return the copy, with selection for multiple tickets. No new issuance or arbitrary anonymous outbound delivery.
- Authenticated admin-only attestation enables one backend Meta utility-template submission after commit. Standard-library HTTPS, worker thread, 8-second socket timeout, server-configured template/language, token number then counter name as body parameters. Existing bot notification helpers were inspected but not reused because they implement different hard-coded templates. No text fallback outside the 24-hour window and no new HTTP dependency.
- Issuance returns separate notification status: not_requested, action_required, unavailable, accepted, failed, unknown. Accepted means Meta message ID, not confirmed delivery. Failure/timeout never rolls back token or retries issuance/delivery. Post-commit queue broadcast failures also no longer produce failed-issuance responses. Copy outcomes/consent are not persisted; no outbox, receipt processor or exactly-once guarantee introduced.
- Both forms have unchecked, labelled consent controls, accessible hints/errors and international phone validation. Admin wording explicitly attests customer consent and preserves issued token on failed/unknown notification. Public UI explicitly says nothing sent yet and requires the user handoff; removed the misleading post-issuance updates/new-token link. Preserved select labels and advisory prediction behavior.
- Config forwarded in local and ignored production Compose files. Durable integration/config/template-order/security/cost docs are in tracked-eligible `backend/README.md` (other markdown names are ignored by this repo). Actual credentials/environment files were not read or changed.
- Verification: 21/21 SQLite API tests passed (15 token-copy plus 6 existing bot lookup tests), 56/56 bot tests passed, bot TypeScript build passed, frontend production build passed, 8/8 real-form mocked browser scenarios passed using Edge at desktop/mobile viewport sizes. Browser tests live in `backend/tests/token-copy-ui.cjs` to honor frontend ownership. Targeted ESLint: zero errors, pre-existing react-hook-form watch/compiler warning only. API tests cover auth/role/tenant, defaults/consent/normalization, public no-send/rate limit, missing config, exact template payload, rejection, timeout, malformed success, one committed token and broadcast failure.
- Environment limits: Docker CLI unavailable in this shell; isolated temporary Python venv used with cwd outside workspace and SQLite. Windows Application Control blocked SciPy DLL loading, so unrelated `app.ml.predictor` was mocked at test-runner import time; actual token/queue/heuristic/API/DB logic exercised, ML inference not retested. Combined suite initially exposed existing bot-test import-order config dependence; fixed its test setup to patch its fake internal key per test, and reran 21/21 successfully. All Meta/HTTP browser traffic mocked, no live sends or production writes/restarts/migrations. No real credentials used.
- Setup remains required: actual bot number and signed webhook; Meta credentials, supported API version, approved utility template with two body parameters and exact language; review paid Meta rates/spend limits and genuine recipient consent. Public copies only cover currently active phone-linked tokens. Persisted wrong/shared/recycled numbers and response-loss/manual retry duplicate issuance remain existing risks. Memory and production Compose stay ignored by existing Git rules.

## Session 2026-09-16: Guarded Backend Model Integration

### Task Tracking
- [x] Read rules, PRD, architecture, phases, memory, all ML notebooks and pipeline modules; traced predictor, optimizer, prediction API, public/bot tickets, staff ETAs, and frontend use.
- [x] Verified initial worktree clean; retained all packaged models and existing live estimator behavior.
- [x] Implemented explicit shadow inference status, contract/input validation, correct service encoding, and tenant-scoped queue counts.
- [x] Repaired candidate training feature selection and evaluation/output boundaries; added tests and tracked model README.
- [x] Verified isolated backend APIs, real RF/LR inference, temporary candidate serialization, and frontend types/lint.
- [ ] Live ML promotion remains blocked by incompatible historical artifact and missing original training data, not marked complete.

### Already Present Versus New
- Already present: RF/LR joblib artifacts, metadata/metrics/peak tables, Docker packaging, lazy predictor, optimizer helper, authenticated `/predictions/wait`, customer-facing empirical/heuristic estimates. No new optimizer endpoint or customer ML rollout was needed or added.
- Discovered a train/serve mismatch beyond the previously documented numeric-ID OOD problem: prefix-based training feature selection included post-service `service_time_min` as a service dummy. Predictor supplied zero in that slot and used `[8:]` for the seven-character `service_` prefix, preventing normal category matches. Importer institution IDs also overlap across sources. Hospital rows lack queue length, imputed to zero without the missingness flag in model features.
- New `Predictor.evaluate` returns a typed shadow result with status, validates feature order/allowed contract, rejects the historical artifact BEFORE joblib loading, rejects unknown categories/nonfinite/out-of-range inputs, checks fitted dimensions, and fails on sklearn version mismatch. Fixed category encoding. Legacy numeric predictor interface retains historical peak fallback, but API and optimizer no longer use that fallback as ML output.
- `/predictions/wait` retains live values and adds `ml_status`, `ml_mode: shadow_only`; `ml_estimate_min` is now nullable rather than returning invalid model/fallback numbers. This intentional API correction is reflected in frontend types. UI still displays only live estimates. Explicit tenant predicate added to queue-count query; same queue count reused for shadow evaluation. Optimizer leaves invalid predicted wait null; allocation math unchanged.
- `pipeline/train.py` now verifies actual category dummy columns rather than selecting every `service_*` column. Category vocabulary uses the chronological training partition; temporal CV uses training rows only, not the final holdout. Writes RF and LR candidates to ignored `ml-notebooks/data/candidates/`, never silently replaces deployed artifacts. Numeric IDs retained only for offline historical comparison; this is NOT a new identity-free validated model. Removed unsupported hard-coded statistical-significance/performance assertions from future training output.
- Notebook historical outputs preserved with an audit warning. Added tracked `backend/app/ml/README.md` covering API contract, artifact trust/deployment, audit, baseline limits, evaluation requirements and test workflow. Updated Phase 8 honestly; neither deployment nor live model promotion is claimed complete.

### Evidence And Verification
- Existing historical metrics, not recomputed: RF MAE 30.84 / RMSE 43.20 / R2 0.1285; LR MAE 31.38 / RMSE 43.30 / R2 0.1244. Shuffled CV RMSE 41.90 / 41.71. These do not validate corrected inference; no formal significance test supports the old "statistically indistinguishable" claim.
- Real tracked artifacts loaded successfully under sklearn 1.9.0 in a disposable backend-image container. Unseen institution 9001/counter 9002, Monday 10:00 UTC, legacy zero service vector: queues 0/1/5/10 produced RF 51.36/51.36/5.82/5.82 min; LR raw -244450.96/-244450.95/-244450.90/-244450.83. Confirms unsafe behavior; not accuracy measurements.
- Final isolated unittest run: 26/26 passed (20 new + 6 existing bot tests). Includes HTTP authentication/tenant/inactive checks, increasing/zero queue fallback, tenant-local empirical activation, nullable failure status, successful shadow never overriding live, public ticket behavior, malformed inputs/metadata/models, feature ordering, optimizer staff totals, real RF/LR smoke, and candidate round trip.
- Candidate round trip trained only a 40-row artificial unit-test fixture with two trees in a temporary container directory. Its metrics were suppressed and are NOT product performance evidence. Verified holdout-only category and post-service duration excluded and deployed model bytes unchanged. No original-data retraining occurred: `ml-notebooks/data/` contains only `.gitkeep` in this checkout.
- Frontend dependencies installed with `npm ci --ignore-scripts --no-audit --no-fund` (no lockfile change). Initial typecheck lacked generated `LayoutProps`; `next typegen` then `tsc --noEmit --incremental false` passed. Targeted ESLint: zero errors, one pre-existing react-hook-form `watch()` compiler warning. No full frontend production build run.
- `git diff --check` passed (LF/CRLF warnings only). Backend tests ran in SQLite memory with repository mounted read-only, cwd `/tmp` to avoid environment files, and networking disconnected after installing httpx. Starlette emitted a test-only httpx deprecation warning. No production DB access/writes, migrations, outbound messages, secret-file output/edits, or live service restarts. Disposable test container removed after verification. No commits or staging.

### Remaining Limits
- Restore original trusted data before corrected-model evaluation. A deployable candidate needs inference-time/identity-free features, missing-queue handling, service-category and time/target semantics, chronological and held-out-tenant/source testing, comparison to the empirical/heuristic baseline, and prospective live outcomes. Removing IDs alone is not evidence ML will improve estimates. Do not just edit metadata or enable the historical RF.
- Existing baseline pools service rates across the institution and uses called-to-completed time. Next-token endpoint counts waiting/called, whereas public/staff positions include in-service work. These pre-existing limitations are documented, not silently recalibrated here.
- Only trusted operator-controlled packaged pickle/joblib is supported, never external uploads. Contract/dimension checks are not a security sandbox or proof of authenticity. Model and metadata cache require process restart on replacement. Artifacts already tracked and Docker-copied; no new packaging/download service or hash-authenticity claim added.
- `memory.md` and `phases.md` remain Git-ignored by existing rules; updated locally. The tracked ML README preserves the integration findings for future checkouts.

## Session 2026-09-15: WhatsApp Flow Repair

### Task Tracking
- [x] Inspected git status and all initial uncommitted changes (checkStatus.ts and handler.ts); incorporated their token-format/scoping and saved-token intent rather than reverting them.
- [x] Reviewed bot join/status/support/notification flows against public API contracts and internal authentication.
- [x] Implemented secure phone lookup, automatic single-ticket display, multiple-ticket selection, pagination, navigation, and truthful support/cancellation messaging.
- [x] Added regression tests and ran bot build/tests plus isolated backend API tests.
- [x] Recorded actual outcomes and deployment/security limits here.
- [ ] Live deployment/smoke: Docker daemon was unavailable; Meta/Redis/live Postgres were not contacted.

### Implemented
- Root causes: saved status button text exceeded Meta's 20-character maximum; status entry defaulted to a non-input step; duplicated public lookups omitted mandatory institution scope and expected nonexistent id/institution_id/queue_position fields. Institution/counter lists invented zero counts from absent fields.
- New authenticated POST /api/v1/internal/bot/tokens/lookup accepts phone in JSON (not URL logs), optional token ID/number, and offset. Returns eight tickets plus has_more, with explicit token/institution IDs and institution name. Every query, including selected ID, remains phone-scoped and excludes inactive institutions. Reuses the actual public ticket builder and position/estimated_wait_min fields.
- Lookup covers existing issued tokens from any source with the same stored full international phone number, including terminal history, across active institutions; formatting punctuation is normalized in SQL. No dependency on Redis session lifetime. Missing/local-only/wrong phone numbers are not guessed or recoverable via chat. Manual number entry filters only the sender's phone-linked tickets; it does not enable arbitrary phone/tenant enumeration.
- Status now automatically displays a sole match; multiple matches use ID-based selection, newest-first, with previous/next pages. Duplicate token numbers across counters/institutions remain distinct in the bot.
- Consolidated institution loading and status lookup. Institution/counter lists show type instead of fabricated counts; paginate eight options plus up to two navigation rows; preserve full server names in session while truncating display fields to Meta limits. Client bounds text/header/body/footer/list/button labels; rejects oversized unpaginated lists/button collections.
- Join uses public position (including zero-minute estimates) and the known institution scope, never nonexistent public IDs. Saves successful issuance state before outbound delivery. All bot backend fetches have 10-second timeouts; read-receipt failures no longer block commands.
- Global status/join/support/menu navigation works during other flows. Support token issues use the same automatic lookup. Cancellation and human forwarding are explicitly unsupported, with staff contact guidance; no fictitious cancellation or escalation is claimed. Notification zero values preserved; notification functions remain helpers, not newly wired backend delivery.
- Webhook now requires raw-body HMAC-SHA256 verification via WHATSAPP_APP_SECRET before parsing/handling; missing/invalid signatures rejected with 401. Inbound business phone-number ID must match configuration. Internal key comparison is constant-time and fails closed for blank/missing keys. Removed incoming phone/message logging and raw Axios error propagation; Meta failures log only status/code.
- Tracking links use optional PUBLIC_WEB_URL, not Docker's backend hostname. Empty/unset URL omits links. Both local compose and the Git-ignored production compose forward new settings. WHATSAPP_APP_SECRET is required in every environment (Meta App Secret, NOT webhook verify token); configure it before restarting. Set PUBLIC_WEB_URL to the reachable frontend origin for live links. No environment secret/example files were printed or edited.

### Verification And Limits
- npm.cmd test: 55 tests passed across 7 files (session helpers, navigation including Check Status button and business-phone filtering, join public contract, automatic status, pagination, support, Meta limits, signature rejection).
- npm.cmd run build: passed. git diff --check: passed (only local LF/CRLF warnings).
- Backend: 5 unittest API tests passed using an isolated temporary venv and in-memory SQLite, emulating PostgreSQL regexp_replace. Covers internal auth, formatted existing phones, multi-institution pagination, no PII, ID ownership/visibility, input validation, and real active-ticket position/zero wait. No live DB writes or outbound WhatsApp messages. PostgreSQL execution and full-container integration remain unverified.
- npm.cmd run lint is blocked by the pre-existing absence of an ESLint configuration. Docker compose inspection failed because Docker Desktop's Linux engine is unavailable; no images rebuilt/restarted.
- Security boundaries: INTERNAL_API_KEY is the existing shared service credential (including CV); holders have cross-institution phone lookup authority. Keep it secret, restrict the internal route at the network edge, and rotate any previously exposed credentials. Signing proves control of the current WhatsApp sender, not that historical phone data was verified at issuance; recycled/shared numbers and incorrectly entered phones remain attribution risks. Only PII-free ticket details are returned, not customer names/phones.
- Existing deferred limitations: no durable webhook work queue or exactly-once/idempotent token issuance, so retries/concurrent messages and response-loss ambiguity remain possible. Public ticket numbers can collide within an institution; bot selection uses true IDs, but public tracking links retain the existing latest-number-match semantics. Phone normalization currently scans stored phone expressions; consider a normalized indexed column as data grows.
- memory.md and docker-compose.prod.yml are Git-ignored by existing rules; updated locally but not included in normal git diff/status. No commits, staging, secret changes, DB resets or unrelated work were performed.

## Session 2026-09-15: Active-Only Bot Status Verification

- Updated only the stale empty-result assertion in checkStatus.test.ts to match active-only wording; no application logic changed this session. Active lookup includes waiting/called/in_service and excludes served/declined/no_show, including direct selection (supersedes the earlier terminal-history note).
- Backend isolated unittest: 6/6 passed using in-memory SQLite in a disposable Docker container. Local FastAPI was missing; installed httpx2 only in that container, disconnected its network before tests, then removed it.
- WhatsApp bot: npm.cmd test passed 55/55 across 7 files; npm.cmd run build passed. git diff --check passed with LF/CRLF warnings only.
- Rebuilt backend and whatsapp-bot images and recreated only those services with --no-deps. Both /health endpoints returned HTTP 200. Used temporary override C:\Users\a7reh\AppData\Local\Temp\opencode\ai-qms-active-only-compose.yml to start backend directly with uvicorn, bypassing Alembic to avoid DB writes. Normal Compose recreation without that override restores migration startup.
- No live DB test writes, message sends, or secret output. PostgreSQL lookup execution was not tested. Unrelated cv-service observed restarting and left untouched.

## Completed
- [x] Proposal document finalized
- [x] Draft directory structure sketched (`ai-qms/`) — reviewed and confirmed fine to build against as-is
- [x] `prd.md`, `architecture.md`, `rules.md`, `phases.md`, `design.md` drafted
- [x] ML approach decided: Random Forest (primary) + linear regression baseline for wait-time prediction, pandas aggregation for peak/off-peak detection
- [x] Auth strategy decided: JWT access + refresh tokens, bcrypt password hashing, admin/staff roles, institution-scoped access — see `architecture.md` §5
- [x] **Phase 0 — Project Setup complete.** Repo scaffolded per `architecture.md` §3 and verified with `docker compose up`:
  - `backend/` — FastAPI skeleton (`app/main.py` with `/health`, `app/config.py` Pydantic Settings, `app/database.py` SQLAlchemy), empty packages `models/ schemas/ api/v1/ services/ ml/ websocket/ utils/`, `requirements.txt`, `Dockerfile`, `alembic.ini`, empty `migrations/` + `tests/`
  - `frontend/` — Next.js 16.3 (App Router) + TypeScript + Tailwind v4 + shadcn/ui (`base-nova` style) skeleton: base `layout.tsx`/`page.tsx` (AI-QMS landing page), empty `(auth)/` + `(dashboard)/` route groups, `src/lib/` (api-client, socket, utils), `src/hooks/`, `src/store/`, `src/types/`, `package.json`, `Dockerfile` (multi-stage standalone)
  - `whatsapp-bot/` — placeholder `package.json` + `Dockerfile` only, no logic
  - `ml-notebooks/` — placeholder `eda.ipynb` + `wait-time-prediction.ipynb`, empty `data/`
  - `infrastructure/` — stub `k8s/` manifests + `nginx/default.conf` (no-ops until Phase 8)
  - Root — `.gitignore`, `.env.example`, `docker-compose.yml` (**Postgres**), `README.md` (quick-start), `LICENSE` (MIT)
  - Verified: backend `/health` → `{"status":"ok"}`, frontend serves landing page (HTTP 200), backend connects to Postgres inside compose network.
- [x] **Phase 1 — Research & Requirements complete.** Final outstanding item (data sources) decided: **synthetic data now** (unblocks Phases 2–5), Google Form survey + public datasets to be reconciled into the canonical schema once shared. ML approach already decided (Random Forest + linear regression baseline; pandas aggregation for peak detection).
- [x] **Phase 2 — Data Collection & Preprocessing (pipeline built).** Added `ml-notebooks/`:
  - `pipeline/schema.py` — canonical queue-event schema (institution_id, counter_id, service_type, arrival/called/service_start/service_end ts, queue_length_at_arrival), `PipelineConfig`, outlier bounds, null policies
  - `pipeline/generator.py` — synthetic generator with realistic peaks/missing values/queue length (seeded, reproducible)
  - `pipeline/cleaning.py` — timestamp normalization (UTC), missing-value policy (drop incomplete services, flag null queue length), derived `wait_time_min`/`service_time_min`, outlier winsorize-or-drop, time features (hour/day/weekend), categorical encoding (one-hot default, label option)
  - `requirements.txt` (pandas, numpy, scikit-learn) + `ml-notebooks/.venv` (local only)
  - `data-engineering.ipynb` — step-by-step walkthrough
  - `data/raw/queue_events.csv` + `data/processed/queue_events_clean.csv` generated (git-ignored, reproducible)
  - Verified run (synthetic): 15,267 raw → 13,430 clean rows, zero nulls.
- [x] **Phase 2 — real collected data integrated.** User placed 3 files in `ml-notebooks/data/collected/`:
  - `Hospital Wait  TIme Data.csv` (5,000 rows) — hospital dataset; mapped: ActualArrivalTime→arrival_ts, TriageCompleteTime→called_ts, ProviderStartTime/EndTime→service_start/end_ts, Department→service_type, ProviderID→counter_id (factorized; was non-numeric). No queue-length column — flagged missing; `facility_occupancy_rate`, `triage_category`, `age_group` kept as passthrough features.
  - `queue_data.csv` (560 rows) — queue log; `D-M-YYYY H.MM` parse; source `wait_time` verified to equal `finish−start` (560/560) so canonical wait derived as `service_start−arrival` instead; source value kept as `reported_wait_time` passthrough.
  - `Smart_Queue_Management_Survey.xlsx` (220 rows) — Google Form survey; self-reported wait/service/people-ahead buckets → midpoints (open buckets: upper+10); service_start=arrival+wait, called=service_start; institution factorized from organization type; purpose→service_type.
  - Added `pipeline/importers.py` (one importer per source + `combine()`), updated `data-engineering.ipynb`, added `openpyxl` to requirements.
  - Verified run: 5,780 raw → **5,768 clean rows, zero nulls in required columns** (hospital 5,000 / queue_log 560 / survey 208; 19 service_types).
  - Raw+processed outputs in `data/raw/queue_events_real.csv` + `data/processed/queue_events_clean.csv` (git-ignored).
  - **Pending (external):** none blocking — model features (Phase 5) will use these cleaned data plus passthrough columns.

- [x] **Phase 3 — Backend Foundation complete.**
  - Models (`backend/app/models/`): `User` (role admin/staff, `refresh_token_version`, institution FK), `Institution`, `Counter`, `Personnel`, `Token` (status enum: waiting/called/in_service/served/no_show)
  - Alembic wired: `migrations/env.py` reads DB URL from app settings, `script.py.mako`; initial migration `adf74866a5d4` (autogenerated against live Postgres). Verified downgrade→upgrade round-trip in dev (fixed PG enum `token_status` not being dropped on downgrade — added `DROP TYPE IF EXISTS`).
  - Auth (`api/v1/auth.py`): register (creates Institution + admin), login, refresh (rotation via `refresh_token_version`, old tokens rejected after use), me; bcrypt via passlib; PyJWT HS256; access 15 min, refresh 7 days
  - Role + tenant scoping (`api/deps.py`): `get_current_user`, `require_roles`; every query/endpoint scoped to `user.institution_id` (e.g. `/institutions/me` GET/PATCH/DELETE; only admins modify)
  - Login rate limiting: in-memory sliding window per IP (5 per 5 min → 429), per architecture §5
  - `schemas/`: user, auth, institution. Deps added: passlib[bcrypt]==1.7.4 + bcrypt==4.0.1 (abi3 wheels, verified on 3.12/3.14), PyJWT, email-validator
  - Verified end-to-end via HTTP: register/login/me/refresh-rotation/bad-token-401, institution PATCH, rate-limit 429, staff-role 403 on admin action, cross-tenant isolation (admin of institution 2 sees only institution 2)
  - Dev DB reset to clean migrated state after tests.
  - **Note:** containers deprecated build context (no bind mount) — migration files generated inside a container must be `docker compose cp`'d out before committing.

- [x] **Phase 4 — Core Queue/Token APIs complete.**
  - `services/token_service.py` — issue tokens: per-counter `PREFIX-NNNN` numbering (prefix from counter name, seq per prefix so renames don't collide), queue position
  - `services/queue_service.py` — state machine (`call`→`start`→`complete`, `no-show`), `token_position` (non-terminal earlier tokens on same counter), `snapshot` (live queue per active counter, positions, counts)
  - `api/v1/counters.py` — admin CRUD; deactivate (DELETE) blocked with 409 while counter has non-terminal tokens; staff read-only
  - `api/v1/tokens.py` — issue (staff+admin), list (counter/status filters, history newest-first), transitions `/call` `/start` `/complete` `/no-show`; every mutation broadcasts the queue snapshot to the institution's WS subscribers
  - `api/v1/queue.py` — GET live snapshot, optional `counter_id` filter (validated owned)
  - `websocket/manager.py` + `websocket/routes.py` — `/ws/queue` authenticated via `?token=` access JWT; per-institution broadcast; reject connection 1008 if missing/invalid
  - Verified 31 checks via HTTP+WS: numbering/positions, transition 409 guards (complete-on-waiting, call-on-called, no-show-on-served), staff 403 gating (role flipped in DB), cross-tenant 404s, WS broadcast + reject. No model changes → no new migration. Dev DB reset to clean migrated state after tests.

- [x] **Phase 5 — ML Model Development complete.**
  - `ml-notebooks/pipeline/train.py` — chronological 80/20 split, tuned RF (`max_depth=6`, `min_samples_leaf=20`, 150 trees) vs `LinearRegression`, test + 5-fold CV metrics, winner/artifacts written to `backend/app/ml/models/`
  - **Empirical finding: RF and LR statistically indistinguishable on the noisy self-reported data (test RMSE 43.20 vs 43.30 min, R² ~0.13; CV 41.90 vs 41.71)** — too noisy to beat a strong baseline (survey midpoints, 3 mixed sources). Per architecture §2, tuned RF promoted as `wait_time_model.joblib`; LR kept as `wait_time_baseline_linear.joblib` for the Phase 9 improved-vs-traditional comparison. XGBoost/LightGBM skipped (5.8k rows too small).
  - `ml-notebooks/pipeline/peaks.py` — pandas groupby (hour × day-of-week): mean wait, count, `is_peak` (mean ≥ overall mean, ≥5 samples) → `peak_table.csv` + `peak_summary.json`. Peak hours flagged 7–17.
  - `backend/app/ml/predictor.py` — lazy-loaded promoted model, one-hot service encoding (+ fallback for unknown), input clamps (hour/day/queue length), rule-of-thumb fallback (peak-table bucket mean → overall mean), predictions clamped to [0, 240]. Verified in container: weekday peak 16.3 min vs weekend night 7.5, unknown inputs fall back, never raises.
  - `backend/app/ml/optimizer.py` — `peak_factor` (bucket mean / overall mean, clamped 0.5–3.0), `suggest_allocation` distributes available staff across counters via largest-remainder on demand `(waiting + in_service*0.5 + 1) * peak_factor`, per-counter `predicted_wait_min` for the next token (soft-fail None). Verified: totals always equal staff, allocation favours busy counters.
  - `wait-time-prediction.ipynb` — placeholder replaced with real executed notebook (training, CV, peak table, outcome).
  - Backend `requirements.txt` now pins `scikit-learn==1.9.0` (+ pandas) — pinned to the training venv's exact sklearn so joblib artifacts load across envs.
  - **Note (Phase 8 integration):** live `Token`/`Counter` rows carry no `service_type`/explicit features needed by the model — endpoints must map (e.g. counter type → service_type) and accept guessed queue length; brand-new institution/counter IDs are out-of-distribution for the model.

- [x] **Phase 6 — Web Application complete.**

- [x] **Phase 7A — WhatsApp Conversational Bot complete.**
  - `whatsapp-bot/` — full TypeScript service (Express, multi-stage Dockerfile)
  - Webhook: `GET /webhook` (Meta verification) + `POST /webhook` (inbound messages, statuses)
  - Session manager: Upstash Redis, per-phone state machine, TTL + hourly cleanup
  - Graph API client: text, template, interactive list/buttons, mark-as-read
  - Conversation flows:
    - Join Queue: institution list → counter list → optional name → confirm → POST /public/tokens
    - Check Status: token number lookup via GET /public/tokens/{number}
    - Support: queue issue / token problem / human escalation
  - Notifications (template-based): called, served, rescheduled, cancelled, queue_update
  - Integrates with existing public APIs: `/public/institutions`, `/public/counters`, `/public/tokens`, `/predictions/wait`
  - Added to docker-compose.yml (port 3001, depends on backend)
  - 12 unit tests passing (session helpers)
  - Note: Meta business verification pending; mock mode recommended for local dev

- [x] **Phase 7B — CV Queue Detection complete.**
  - `cv-service/` — Python service (YOLOv8n person detection + ByteTrack tracking)
  - Detector: `detector.py` — YOLOv8n on CPU, person class only, configurable confidence/IoU
  - Tracker: `tracker.py` — ByteTrack wrapper with ROI entry/exit detection, dwell time
  - ROI Manager: `roi.py` — Polygon regions per counter, loaded from JSON config
  - Analytics: `analytics.py` — Queue length, service rate (people/min), dwell time, estimated wait
  - Sender: `sender.py` — POST /cv/queue-update with retry/timeout handling
  - Main loop: `main.py` — Camera capture → detect → track → analyze → send (throttled)
  - Backend integration:
    - Counter model: cv_enabled, cv_queue_length, cv_service_rate, cv_estimated_wait_min, cv_last_update, camera_url, roi_polygon
    - Endpoint: `POST /api/v1/cv/queue-update` with X-Internal-API-Key auth
    - WebSocket broadcast: `cv_update` event to institution subscribers
    - GET `/api/v1/cv/counters/{id}/cv-status` for dashboard
  - Dockerized: multi-stage Dockerfile with OpenCV dependencies, resource limits (2 CPU, 2GB RAM)
  - Added to docker-compose.yml with environment config
  - Tested: CV endpoint updates database and broadcasts via WebSocket

- [x] **Phase 7C — CV Frontend Integration complete.**
  - Types: Added `CVQueueUpdate`, `CVStatus`, `cv_*` fields to `Counter`, `CounterBoardEntry`
  - WebSocket: `cv_update` frame type in `SocketFrame`, `subscribeCVUpdate()` in `QueueSocket`
  - New hook: `useCVUpdates()` for real-time CV data via WebSocket (with counter filtering)
  - New hook: `useCVStatus()` for REST API status fetch
  - Admin dashboard: Counter cards show CV queue length with pulsing indicator + service rate
  - Management counters tab: Added "CV Queue" column with live count + service rate
  - Mobile cards: Show CV queue length with animated indicator
  - Backend overview service: Include CV fields in `CounterBoardEntry` for admin dashboard
  - **Frontend foundation (carried from earlier in the phase):** `types/` (User/Institution/Counter/Personnel/Token/QueueSnapshot/AuthResponse), zustand auth store (`store/auth.ts`, persisted, login/register/refresh-with-rotation/logout), `lib/api-client.ts` (Bearer header + silent 401→refresh→retry), `lib/socket.ts` (WebSocket manager: JWT query-param auth, per-listener dispatch, 2s auto-reconnect, no reconnect when logged out), hooks `use-queue.ts` (WS + 30s polling fallback, connected flag) + `use-resources.ts` (counters/personnel/institution/tokens w/ filters), shadcn `ui/` primitives (alert/badge/card/dialog/input/label/select/separator/skeleton/status/table), auth forms (login/register).
  - **Backend addition for this phase:** `api/v1/personnel.py` + `schemas/personnel.py` (admin CRUD, soft-deactivate, counter assignment w/ tenant scoping; staff read-only), wired in `main.py`.
  - **Pages built (all client components, Next.js 16 App Router, Turbopack):** `(auth)/` login + register (+centered layout); `(dashboard)/layout.tsx` auth guard (redirects to /login when no token; renders skeletons meanwhile) with fixed sidebar + topbar (institution name via `/institutions/me`, user, logout via router); `overview` (4 KPIs: waiting/in-service/served-today/no-shows-today + compact live panel + personal card); `live-queue` (per-counter panels with positions, status badges, call/start/complete/no-show actions, live-updated via WS broadcast); `tokens` (issue-token form w/ counter select + history table w/ counter/status filters and inline transitions); `counters` + `personnel` (admin CRUD dialogs, admin-gated actions, staff read-only); `analytics` (KPIs from token history: today's served/no-show/abandonment, avg issue→call and issue→completed times, hour-of-day CSS bar chart, peak hour, per-counter table); landing `page.tsx` is now auth-aware (Sign in / Create institution vs. Open dashboard).
  - **Backend fix discovered during testing:** `POST /auth/register` returned raw 500 on duplicate institution name (unhandled IntegrityError at flush) — now caught and mapped to 409; email-duplicate path already 409'd. Verified all three register outcomes (fresh 201, dup name 409, dup email 409).
  - Verified: `npm run build` clean (9 routes statically pre-rendered), `npm run lint` clean after fixing `react-hooks/set-state-in-effect` violations (layout uses `useSyncExternalStore` for mounted; `use-resources.ts` fetches in effect w/o sync setState) and `@next/next/no-location-assign-relative-destination` (router-based navigation in forms/topbar/logout). End-to-end smoke test (28/28) via HTTP+WS against the running compose stack: register→me, counter+personnel CRUD, token issue (`TEL-0001`-style per-counter prefixes), positions, transitions + 409 guards, WebSocket broadcast on mutation, status/counter filters, refresh rotation, cross-tenant isolation.
  - **Known limitations (deferred):** `/tokens` list has no date/pagination params — analytics computes "today" client-side by filtering the full list (fine at this scale, revisit with a real analytics endpoint); no backend analytics endpoint (Phase 6 scope was the UI); wait-time predictions from `ml/predictor.py` are not yet surfaced in the UI (needs the endpoint mapping noted in Phase 5's Phase-8 note); **no endpoint/UI to create staff accounts** for an existing institution (register only makes admins — `personnel.user_id` is never set via the UI); docker frontend image rebuilt with the new pages; compose left running; DB reset to clean migrated state after tests.
  - **Note:** all Phase 6 work (personnel API + entire frontend auth/dashboard) is **uncommitted** on `main` (previous session left it in the working tree).

## In Progress
- **Phase 8 (Deployment)** — Oracle Cloud Free Tier provisioning, CI/CD pipeline (GitHub Actions), Cloudflare Tunnel + SSL, managed Postgres (Neon) + Redis (Upstash), Docker Compose or k3s on Oracle VM. WhatsApp test number setup for demo.

## Session additions 2026-08-24 (Phase 7-v2, branch `role-based-institution-UI`)

**Role-based workflow split.** Admin and staff now have separate shells/pages; institution short codes anchor a two-step login.

- **Institution codes:** `institutions.code` = 3 name-derived letters + zero-padded id (`SHR016` style), generated post-flush in register, returned by `POST /auth/register` (`RegisterResponse`), shown on the success screen + admin settings. Column stays **nullable at schema level on purpose**: derivation needs the row id, so the value is written in the same transaction after INSERT (NOT NULL broke register with NotNullViolation — found via smoke). UNIQUE index is the seatbelt. Backfill logic inlined in the migration (must not track app util refactors).
- **Login:** requires `institution_code`; new public rate-limited `POST /auth/institution/verify` returns `{name,type,is_active}`. Login also rejects when the *institution* is inactive (was a real hole: deactivated tenants could still sign in). Case-insensitive code matching.
- **Presence:** `personnel.work_status` (`off_duty|available|on_break`, declared by staff via `PATCH /staff/status`); "serving" derived from tokens stamped `served_by_personnel_id` on call/start. Roster broadcast over WS as tagged envelope `{event:"presence",data:{entries:[...]}}`; queue frames are now `{event:"queue",data:{snapshot}}`. FE socket routes envelopes, falls back to legacy untagged snapshots.
- **Staff self-service router** (`/staff/*`, gated on linked active Personnel row, not raw role): workspace one-shot payload (counter+queue+today stats w/ `tz_offset_minutes`), status, counter claim (`SELECT…FOR UPDATE` occupancy check → clean 409).
- **Decline:** new terminal token status + reason column; allowed from waiting/called/in_service. Analytics: `declined` counts additive everywhere; `avg_total_min` now SERVED-only (no-shows previously inflated it).
- **Admin overview endpoint** `/overview/admin`: funnel, today totals, counter board, staff roster, hourly issued-vs-completed series — single round-trip for the dashboard page.
- **Personnel:** create-with-account atomically (single form creates person+login); `user_id` removed from PersonnelCreate (it accepted arbitrary cross-tenant user ids); added admin reset-password (bumps refresh version) and set-login enable/disable endpoints.
- **Institution purge:** `POST /institutions/me/purge {confirm_name}` hard-deletes; must use Core-level DELETE — ORM parent-delete tried to NULL counters.institution_id and hit NotNullViolation before DB cascade fired.
- **Latent bugs fixed:** `/display` route never existed ((display) group collided with root page.tsx — Next silently dropped it; sidebar link was a 404) → real path `app/display/page.tsx`; B2/B3/B4 above.
- **Frontend structure:** `(admin)` dashboard/management/tokens/analytics/settings · `(staff)` workspace · shared `/settings` (role-rendered content — separate group pages collided on /settings) · compat redirects for /overview /live-queue /counters /personnel · shared primitives (PageHeader, StatCard, StatusDot, EmptyState, ConfirmDialog w/ type-to-confirm, base-ui Tabs, MobileNav slide-over in unified DashboardShell) · dependency-free SVG hourly chart + funnel tiles · two-step login form w/ verify card + last-code prefill · staff workspace (presence control, counter switcher, now-serving hero w/ live elapsed timer, rich up-next rows w/ eta_min, decline dialog) · management tabs deep-linkable via ?tab=.
- **Verification:** Alembic revision `c8f4e2a91b73` round-trip + backfill verified; REST smoke suite 75/75 incl. parallel claim race, purge cascade, cross-tenant isolation, deactivate-blocks-login; WS envelopes asserted from inside container; FE lint 0 errors (2 pre-existing-style watch warnings), build clean 17 routes incl. `/display`.
- **Gotchas for next session:** run-smoke harness patches compose env for rate limits then reverts (container recreate wipes hot-copied files — always `docker compose build` before smoke); PG enum labels are member NAMES (added `DECLINED` uppercase to match WAITING/...); `eta_min` must be computed from ONE service-rate query per snapshot (estimate_wait_minutes is per-call expensive).

## Session additions 2026-08-21 (Phase 7 groundwork)

- **P0 bug fixes:** docker-compose `NEXT_PUBLIC_API_URL` now includes `/api/v1` (was 404ing all API calls in the containerized stack); frontend `register()` now fetches `/auth/me` immediately (fresh admins previously had null user → role-gated admin UI hidden); api-client uses single-flight refresh (`refreshOnce`) so concurrent 401s share one refresh call instead of causing spurious logouts via rotation race.
- **Public customer APIs** (`api/v1/public.py`, anonymous + rate-limited): `GET /public/institutions` (active only, includes whatsapp_number), `GET /public/institutions/{id}/counters` (active), `POST /public/tokens` (issues PII-free ticket; validates inst/counter active + ownership), `GET /public/tokens/{token_number}?institution_id=` (latest match; no name/phone exposed). Rate limits: issue 10/min/IP, lookup 60/min/IP (`config.py` + `rate_limit.py`). Public issue broadcasts queue snapshot to institution WS subscribers.
- **Predictions endpoint** `GET /predictions/wait?counter_id=` (auth'd, tenant-scoped) wrapping `ml/predictor.py` via new `services/prediction_service.py` (maps counter.type→service_type, live waiting+called count as queue length, soft-fails to fallback).
- **Analytics endpoint** `GET /analytics/summary?from&to&tz_offset_minutes=` — server-side KPIs (issued/served/no-shows/abandonment/avg-wait/avg-total/waiting_now/in_service_now), 24-bucket hourly histogram (tz-shifted client-side), per-counter table with avg waits; range validated (to>from, ≤366d).
- **Tokens list**: now paginated `{items,total}` with `limit(≤500)/offset/issued_from/issued_to` filters. **Breaking shape change** — frontend updated accordingly.
- **Auth additions**: `PATCH /auth/me` (name/email w/ uniqueness check), `POST /auth/change-password` (verifies current, bumps refresh version, returns fresh pair so current session survives).
- **Staff accounts**: `POST /personnel/{id}/account` creates linked staff User (409 if exists/inactive); personnel + counters gained `POST /{id}/activate` reactivation endpoints.
- **Institution model**: added `whatsapp_number`; PATCH accepts explicit null to clear (fields_set semantics). **Migration `b3c1f2a7d9e4`** adds `tokens.customer_phone` + `institutions.whatsapp_number`.
- **Frontend**: full landing page (`app/page.tsx`: hero/features/how-it-works/for-institutions/footer, auth-aware CTAs); new `(public)` route group — `/join` (institution search → counter pick → optional name/phone → ticket screen with wa.me deep link when institution has number, web fallback always available) and `/token/[number]?institution=` (live ticket page, 15s polling, position/people-ahead/est-wait, print/save-PDF via window.print + print-hidden chrome); tokens page (pagination, date-range filters, phone column, direct reload — setTimeout hack removed); analytics page (server summary + Today/7d/30d/custom presets + CSV export of filtered history); overview KPIs from analytics endpoint (timezone-correct); settings page (profile + change-password forms); lobby display `(display)` route group (chrome-free auth-guarded "Now serving" board over WS); dark mode toggle (CSS-driven icons + no-FOUC inline script in root layout, localStorage `aiqms-theme`); sidebar/topbar nav + Settings/Lobby display entries.
- **Socket robustness**: `lib/socket.ts` emits connection state (`subscribeState`); on close it silently refreshes the access token before the 2s reconnect retry (no more infinite rejected-retry loops on expired tokens); `connected` flag now resets on disconnect.
- **Verification**: `npm run lint` clean (restructured effects to derived-loading patterns per react-hooks/set-state-in-effect rule), `npm run build` clean (14 routes). Backend rebuilt + migrated; HTTP smoke suite (~50 checks incl. error paths 400/401/403/404/409/422/429) all passing — note: first harness run's "failures" were a PowerShell hashtable-key detection bug in the test script, not app bugs; WS broadcast + bad-token rejection verified; rate limiter verified exact (10 pass, 11th→429). DB reset to clean migrated state after tests; compose left running; frontend image rebuilt.
- **Wait-time estimator rewritten (2026-08-21, `fbfd148`):** live predictions no longer come from the global RF model — probing showed OOD institution/counter IDs produce backwards numbers (1 ahead → 52 min, 5 ahead → 6.5 min; RF treats IDs as numeric features, new tenants are outside training range). New layered `estimate_wait_minutes`: (1) **empirical** — institution's own mean service time (`completed_at − called_at`, last 200 served, ≥`PREDICT_MIN_SERVICE_SAMPLES`=5 required) × people ahead; (2) **heuristic** cold start — people ahead × `PREDICT_DEFAULT_SERVICE_MIN`=5. `/predictions/wait` now returns `{estimated_wait_min, method: empirical|heuristic, avg_service_min, ml_estimate_min}` — the raw ML number is kept exposed for transparency/Phase 9 evaluation but is not used for live estimates. Public tickets inherit the same estimate. Verified: heuristic linear 5/person from empty queue; empirical activates after serving tokens and tracks the tenant's real pace. The RF model itself remains promoted for Phase 9's improved-vs-traditional comparison on historical data.
- **Frontend perf/robustness pass (2026-08-21, commits through `2cf80b3`):** found + fixed the real cause of "15–30s to load data": `useQueue` never fetched on mount — first data arrived only at the first 30s poll tick or next WS mutation (masked during dev by constant token actions). Now fetches immediately on mount. Audit of all data-layer files also fixed: (1) `useResource` gained a 5s-TTL micro-cache + in-flight promise dedupe (kills duplicate concurrent GETs like Topbar+InstitutionCard both fetching `/institutions/me`; mutations bypass via `reload()`); (2) `auth.refresh()` no longer clears the session on network errors — only on definitive 401 (backend restart/wifi blip must not log users out); (3) WS close handler refreshes the token only on close code 1008 (auth rejection, per `websocket/routes.py`) instead of every close — no rotation spam during outages. CORS: `.env` created with `CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]` so local `npm run dev` works alongside the containerized frontend; `.env.example` API URL corrected to include `/api/v1`.
- **Known notes for Phase 7 proper**: bot provider still undecided (Twilio/Cloud API/BaaS); `wa.me` deep links are already wired client-side once an institution sets its WhatsApp number; token numbers can collide across counters sharing a 3-letter prefix within one institution (pre-existing numbering quirk — public lookup picks latest, but worth fixing properly later).

## Not Started
- Phase 9 (testing/evaluation: technical benchmarks, pilot Alpha/Beta/Gamma, usability/accessibility) — see `phases.md`

## Open Decisions
See `prd.md` §8 and `architecture.md` §6 for detail. Short version:
- Power BI vs. custom dashboard for visualization → **decided: Custom Next.js**
- Scope of the "image processing model" mentioned once in the proposal → **decided: Queue length detection via camera (YOLOv8n + ByteTrack)**
- WhatsApp bot: notifications only, or also token generation → **decided: Full conversational bot (join queue, check status, support)**
- ~~DB engine~~ → **decided: Postgres** (defaulted in Phase 0 for docker-compose, fits multi-tenant design). Hosting still open.

## Phase 0 Deviations / Notes
- **Planning docs location:** `prd.md` etc. live at the repo **root**, not under `docs/` (no `docs/` dir exists). Left as-is.
- **`.gitignore` ignores all `*.md`** (pre-existing rule) — so `prd/architecture/rules/phases/design/memory` are **not git-tracked**. Left unchanged; flagged as a risk to session continuity — decide whether to fix (e.g. `!prd.md !architecture.md !rules.md !phases.md !design.md !memory.md`).
- **Frontend generated, not hand-written:** `create-next-app` (Next.js 16.3, Turbopack, Tailwind v4) + `shadcn@latest init` (new `base-nova` style). Added `.dockerignore` for both `frontend/` and `backend/` — without it, `COPY . .` clobbers the Linux `node_modules`/`.venv` with Windows-built artifacts and the image build fails.
- **Route groups built in Phase 6:** `(auth)/login`, `(auth)/register`, and the full `(dashboard)/` shell (guard layout, sidebar, topbar, overview/live-queue/tokens/counters/personnel/analytics pages). No longer empty.
- **`migrations/` + `tests/` empty** per the Phase 0 task. `alembic.ini` exists but there's no `migrations/env.py` yet — wiring migrations to `app.config.settings` happens in Phase 3.
- **`requirements.txt` uses minimum-version ranges** (e.g. `fastapi>=0.115`) rather than exact pins for Python-version flexibility. Container image: `python:3.12-slim`.
- **Verification:** Docker Desktop initially would not start in this environment; skeleton was verified with local tooling first (venv + uvicorn, `npm run build`), then fully verified with `docker compose up --build -d` once Docker was running. Compose currently left running — `docker compose down` to stop.

## Session Log
| Date | Summary |
|---|---|
| 2026-09-02 | **Phase 7C — CV Frontend Integration merged to `main`.** `cv-frontend-integration` branch merged and pulled locally. All Phases 7A-7C complete. Phase 8 (deployment) started: Oracle Cloud Free Tier, CI/CD, Cloudflare Tunnel, managed Postgres/Redis, Docker Compose on Oracle VM. |
| 2026-09-02 | **Phase 7C — CV Frontend Integration complete.** Frontend types updated with CV fields, WebSocket `cv_update` event handling, `useCVUpdates()` and `useCVStatus()` hooks. Admin dashboard counter cards show CV queue length with pulsing indicator. Management counters tab has "CV Queue" column with live data. Mobile cards show CV queue. Backend overview service includes CV fields in `CounterBoardEntry`. Committed to `cv-frontend-integration` branch. Next: Phase 8 (deployment). |
| 2026-09-02 | **Phase 7B — CV Queue Detection complete.** Built `cv-service/`: YOLOv8n + ByteTrack person detection/tracking, ROI manager with polygon regions, queue analytics (length, service rate, dwell time, estimated wait). Backend integration: Counter model CV fields, POST /cv/queue-update endpoint with internal API key auth, WebSocket `cv_update` broadcast. Dockerized with resource limits. Added to docker-compose. Tested: CV endpoint updates DB + broadcasts WS. Next: Phase 7C (frontend CV integration) / Phase 8 (deployment). |
| 2026-09-01 | **Phase 7A — WhatsApp Conversational Bot complete.** Built `whatsapp-bot/` service: webhook verification/handler, Upstash Redis sessions, Graph API client, join-queue/check-status/support flows, template notifications. Integrated with `/public/*` APIs. Dockerized + added to compose. 12 tests passing. Committed to `main` (dc7f45a). Meta verification pending; mock mode for dev. Next: Phase 7B — CV queue detection. |
| 2026-08-05 | Reviewed proposal doc + directory sketch; created prd/architecture/rules/phases/design/memory docs; flagged 3 open questions between the proposal and the directory structure |
| 2026-08-05 | Decided ML approach (Random Forest + linear regression baseline; pandas aggregation for peak detection); updated `architecture.md`, `rules.md`, `phases.md` accordingly; reviewed directory structure and confirmed no restructuring needed to start |
| 2026-08-05 | Decided auth strategy (JWT + roles + institution-scoped access); updated `architecture.md`, `rules.md`, `phases.md` accordingly |
| 2026-08-05 | **Phase 0 complete.** Scaffolded backend (FastAPI), frontend (Next.js 16 + Tailwind v4 + shadcn/ui), whatsapp-bot, ml-notebooks, infrastructure stubs, root compose/env/readme. Defaulted DB engine to Postgres in docker-compose. Verified `docker compose up` brings up db+backend+frontend; `/health` and landing page respond. Next: Phase 1 |
| 2026-08-05 | **Phase 1 complete.** Dataset-source decision locked: synthetic now, Google Form + public datasets TBD. **Phase 2 built:** `ml-notebooks/pipeline/` (schema, generator, cleaning), `requirements.txt`, `data-engineering.ipynb`; verified end-to-end run (15,267 → 13,430 clean rows, zero nulls). Note: Phase 2/3 rely on synthetic data until user shares survey responses/public datasets. Next: Phase 3 — Backend Foundation |
| 2026-08-05 | **Real data integrated (Phase 2).** User added hospital CSV (5,000), queue log CSV (560), survey XLSX (220) to `data/collected/`. Built `pipeline/importers.py` mapping each onto the canonical schema (documented mapping decisions in `data-engineering.ipynb`); pipeline run: 5,780 → 5,768 clean, zero nulls. Next: Phase 3 — Backend Foundation |
| 2026-08-12 | **Phase 3 — Backend Foundation complete.** Models (User/Institution/Counter/Personnel/Token), Alembic initial migration (verified round-trip), JWT auth (access/refreshing refresh, bcrypt), role + institution-scoped deps, institution CRUD, login rate limiting. Verified: register/login/me, refresh rotation, 401/403/429 paths, cross-tenant isolation. Next: Phase 4 — Core Queue/Token APIs |
| 2026-08-12 | **Phase 4 — Core Queue/Token APIs complete.** `services/token_service.py` (issue tokens, `PREFIX-NNNN` numbering per counter, position) + `queue_service.py` (state machine call→start→complete, no-show, snapshots with positions); `api/v1/counters.py` (admin CRUD, deactivate blocked while tokens active), `tokens.py` (issue/list + transition endpoints, broadcasts), `queue.py` (live snapshot, optional counter filter); `websocket/manager.py` + `/ws/queue` (JWT query-param auth, per-institution broadcast of queue snapshots). Verified 31 HTTP/WS checks: numbering/positions, transition 409 guards, 403 staff gating (role flipped in DB), cross-tenant 404 isolation, WS broadcast + reject. DB reset to clean migrated state. No schema changes → no new migration. Next: Phase 5 — ML Model Development |
| 2026-08-12 | **Phase 5 — ML Model Development complete.** `ml-notebooks/pipeline/train.py` (tuned RF vs LR, chronological + 5-fold CV: RMSE 43.20 vs 43.30 — indistinguishable; RF promoted per architecture §2, LR baseline artifact kept; XGBoost skipped) + `peaks.py` (groupby peak table, peak hours 7–17). `backend/app/ml/predictor.py` (validated + fallback) and `optimizer.py` (staff allocation via largest-remainder) verified in the rebuilt container; `wait-time-prediction.ipynb` executed with outputs; `scikit-learn==1.9.0` pinned in backend requirements to match training venv for pickle compatibility. Next: Phase 6 — Web Application |
| 2026-08-12 | **Phase 6 — Web Application complete.** Full dashboard: auth store + API client (Bearer + 401-refresh-retry) + WS socket manager with reconnect, hooks, shadcn UI primitives, login/register pages, guarded dashboard shell (sidebar/topbar), and 6 pages (overview KPIs, live-queue w/ real-time WS + token transitions, tokens w/ issue form + filters, counters + personnel admin CRUD, analytics w/ hour-of-day bars). Added backend `personnel` API (admin CRUD, tenant-scoped). Fixed backend bug: register 500 on duplicate institution name → clean 409 (flush-order bug found via smoke test). Lint + build clean; 28/28 HTTP+WS smoke checks passed (incl. WS broadcast and cross-tenant isolation); DB reset to clean state; compose left running. Next: Phase 7 — WhatsApp Integration |
| 2026-08-12 | **Phase 6 audit pass.** Re-reviewed every page against PRD + backend surface; added: (1) Counter column in tokens history (TokenOut only has counter_id — mapped via counters list), (2) institution management card on overview (`components/layout/institution-card.tsx`, admin-only edit dialog → `PATCH /institutions/me`, filling the PRD "institution management" gap). Flagged remaining gaps (backend work, not Phase 6 scope — see Known limitations): wait-time prediction display (needs endpoint mapping `ml/predictor.py`, deferred by Phase 5 note) and staff-account creation (no endpoint exists). Lint + build clean, frontend image rebuilt, routes re-verified, DB still clean. |
| 2026-08-21 | **Phase 7 groundwork.** Public customer APIs (join/ticket flow), analytics + predictions endpoints, staff account creation endpoint, lobby display, dark mode, P0 fixes (compose API URL, single-flight refresh, mount-fetch in useQueue), wait-time estimator rewritten to empirical/heuristic layers, frontend perf pass. Commits `fdaa7e9`…`2cf80b3` on `main`. |
| 2026-08-24 | **Phase 7-v2 (role-based workflow) implemented on `role-based-institution-UI`.** Institution short codes + two-step login (verify endpoint, code required at login, inactive-institution block), admin/staff shell split (dashboard/management/settings vs workspace), staff presence (declared status + derived serving via served_by stamping), decline-with-reason terminal state, /overview/admin aggregate, personnel create-with-account + reset-password + set-login, institution purge with typed confirm, WS envelope protocol. Fixed latent bugs: missing /display route, cross-tenant user_id hole in PersonnelCreate, deactivated-institution login bypass, avg_total including no-shows. Smoke 75/75 + WS green; lint/build clean; DB reset to clean migrated state; compose left running. Next: merge after review, then Phase 7 proper |
| 2026-08-27 | **Bug-fix sprint on top of Phase 7-v2 (branch `role-based-institution-UI`).** 6 bugs resolved + 2 new discovered. **Fixed:** (1) Staff status realtime on `/management` — added WS presence subscription to StaffTab; (2) Edit staff modal dead button — split create/edit schemas, single typed resolver boundary (removed all `as any`); (3) Counter assignment UX — dedicated "Switch Counter" button per row, "Password" → "Reset Password"; (4) Counter change reliability — fixed PATCH payload + realtime refresh; (5) Staff dashboard "Failed to fetch" after counter switch — request-ID guards in `useWorkspace`/`useAdminOverview`, `forceReconnect()` on token rotation; (6) WS stuck "Reconnecting" — hardened `socket.ts` (1008 handling, await `refreshOnce()`, dev logging). **New bugs found & fixed:** (7) Institution codes with numeric words (`"Hospital 24"`) generated invalid login codes (`HOS016` → regex rejected) — generator + migration backfill now emit letters-only prefixes (skip numeric words); (8) Management crash on null member in reset-password dialog — guarded. **Testing:** 75/75 REST + WS smoke green; lint/build clean; manual verified all flows. PR ready for review. Commits: `e1bde56` (bug fixes), `92cf8f9` (prefix + type cleanup), `85f9474` (10-bug batch), `1889548` (crash guard). |
